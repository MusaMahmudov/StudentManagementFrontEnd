import { createTheme } from "@mui/material";

export const searchTextFieldDesign = createTheme({
  textField: {
    background: "white",
    width: { md: 200, lg: 280, xl: 340, xs: 200 },
    zIndex: 1,
  },
});
